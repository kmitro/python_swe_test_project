from setuptools import setup, find_packages

setup(
    name='kmitro_python_swe_test_project',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your project dependencies here
    ],
    entry_points={
        'console_scripts': [
            # Define command-line scripts here
        ],
    },
)